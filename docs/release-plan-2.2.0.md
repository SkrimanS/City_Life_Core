# Release Plan 2.2.0

Scope: Snapshot and Client Sync Model.

Tasks:

- Full and filtered snapshot summary helpers.
- Visibility scope metadata.
- Snapshot validation and digest output.
- Tests, example, docs and changelog.

Non-goals: network replication protocol, WebSocket, client prediction, rollback netcode, UI.
