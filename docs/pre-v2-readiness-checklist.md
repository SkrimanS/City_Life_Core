# Pre-v2 Readiness Checklist

Use `make_deep_simulation_core_readiness_report` before treating the v1.x line as complete.

Required readiness flags:

- processor architecture ready;
- feature toggles ready;
- dependency toggles ready;
- save/load ready;
- replay ready;
- diagnostics ready;
- engine agnostic ready;
- no validation errors.

