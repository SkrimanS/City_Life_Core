# Release Notes 2.2.0

Added deterministic runtime snapshot summaries for full, public, faction and settlement-scoped views.

The API is intended for future client/tool sync layers while remaining transport-agnostic.
