# Editor/Admin API

The editor/admin integration API is headless and query-oriented.

Use `query_platform_core_registry` and the existing runtime, diagnostics, action, replay and migration reports to build external editor or admin tools. The core does not include an admin UI, account system, permissions service, database browser or web dashboard.
